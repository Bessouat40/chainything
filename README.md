# Chainything

This project allows you to connect multiple processor together in order to create a processing pipeline.

## How it works

Chainything is based on a DAG architecture that constructs a graph based on given processor and then execute all computing.
